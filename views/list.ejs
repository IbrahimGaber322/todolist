<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= day %> </h1>
  </div>

  <div class="box">
    <p>Welcome to your to do list !</p>
        <p>Use the + button to create an item</p>
        <p>Check the box to cross an item</p>
    <% items.forEach(element => { %>
      <form action=<%=action2%> method="post" >
        <div class="item">
        <input type="checkbox" name="checkbox" value="<%=element._id%>" onchange="this.form.submit()" >
        <p><%=  element.name  %></p>
      </div>
      <input type="hidden" name="list" value=<%=day%>>
    </form>
   <% }); %>
    
    

      <form class="item" action = <%=action%> method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value=<%=day%>>+</button>
      </form>

      
  </div>

<%- include("footer") -%>
